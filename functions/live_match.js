const fetch = require("node-fetch");
const HTMLParser = require("fast-html-parser");

exports.handler = async (event, context) => {
    // TODO - Easy, for a live match it has some class related to "live"
}
